// このファイルは自動生成するので手動では編集せず
// package.nls.jsonなどを編集したあと、
// `npm run precompile`を実行すること
/** ロケールごとの文言が用意されているメッセージのキー */
export type LocaleMapKey =
  | 'preview.preview'
  | 'preview.settings'
  | 'configuration.description.disable'
  | 'configuration.description.currentColor'
  | 'configuration.description.size'
  | 'configuration.description.preset';
/** ロケールごとに用意するメッセージのキーと文言のマッピング */
export type LocaleMap = Record<LocaleMapKey, string>;
